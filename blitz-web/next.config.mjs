/** @type {import('next').NextConfig} */
const nextConfig = {
  // Served at https://vinero.app/blitz — keep asset/route paths rooted there.
  basePath: '/blitz',
  reactStrictMode: true,
};

export default nextConfig;
